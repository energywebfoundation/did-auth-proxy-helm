apiVersion: v1
kind: Service
metadata:
  name: {{ include "did-auth-proxy.fullname" . }}
  labels:
    {{- include "did-auth-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "did-auth-proxy.selectorLabels" . | nindent 4 }}
